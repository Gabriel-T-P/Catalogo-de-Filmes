<%= form_with model: movie_genre do | f | %>

<div>
  <%= f.label :name, "Nome: "%>
  <%= f.text_field :name %>
</div>

<br>

<div>
  <%= f.submit "Salvar", disable_with: 'Salvando...'%>
</div>

<% end %>
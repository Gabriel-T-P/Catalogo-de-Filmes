<h1>Todos os filmes com Status: <%= @params %> </h1>

<% @movies.each do | movie | %>
  <div>
    <% if movie.poster.attached? %>
      <%= image_tag movie.poster %>
    <% end %>
  </div>
  <%= link_to movie.title, movie %> -
  Data de Lançamento: <%= movie.release_year %>
  <br>

<% end %>